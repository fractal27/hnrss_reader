# TODO

- Formatting of messages(Example: `$ hnrss_reader --format '{author}: {title} -> {link}'`) needed:
	- getting all the fields at the reading of the format
	- handling of escape of the curly brace
	- dynamic allocation of strings to replace the {..} string.
